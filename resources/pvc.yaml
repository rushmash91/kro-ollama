apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    volume.kubernetes.io/storage-provisioner: ebs.csi.aws.com
  name: ollama-pvc
  namespace: default
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi
  storageClassName: ollama-storage-class
  volumeMode: Filesystem
  volumeName: pvc-90161ce7-903a-4503-a424-da9b1f692662